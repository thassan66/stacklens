spring:
  datasource:
    url: jdbc:postgresql://db.internal:5432/app
    username: app_user
    password: SuperSecretPassword123

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true

logging:
  level:
    root: DEBUG
